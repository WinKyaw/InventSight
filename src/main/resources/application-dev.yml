# InventSight Development Configuration
# Current Date and Time (UTC): 2025-08-26 09:17:13
# Current User's Login: WinKyaw

spring:
  # Development Database Settings (H2 In-Memory)
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  
  # Email Configuration for Development
  mail:
    host: smtp.gmail.com
    port: 587
    username: windaybunce@gmail.com
    password: 12345
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
        from: windaybunce@gmail.com
  
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration
      - org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
  
  # Flyway Configuration for Development
  flyway:
    enabled: false  # Disable Flyway for H2 in-memory development database
    # When using PostgreSQL in dev, enable Flyway with lenient checksum handling
    # Uncomment below when using PostgreSQL:
    # enabled: true
    # validate-on-migrate: false  # Disable validation for development - allows modified migrations
    # repair: true  # Automatically repair checksum mismatches
    # clean-disabled: false  # Allow flyway:clean for development database resets

# Server Configuration
server:
  servlet:
    context-path: /api  # Set /api prefix for all endpoints
        
  # Development Logging
logging:
  level:
    com.inventsight: DEBUG
    org.springframework.security: DEBUG
    org.hibernate: DEBUG
    
# Development Security
inventsight:
  security:
    jwt:
      secret: inventsight-dev-secret-2025-winkyaw
      expiration: 86400000 # 24 hours
      
  # Email Provider Configuration for Development
  email:
    from-address: windaybunce@gmail.com
    from-name: InventSight
      
  system:
    environment: "Development"
    debug: true
    current-user: "WinKyaw"
    current-datetime: "2025-08-26 09:17:13"